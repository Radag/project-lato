{form form id=>"form-user-classification"}
    <div class="modal-content">
        <h3>Přidat známku</h3> 
        {ifset $classificationUsers}
        <p>Přidat známku těmto studentům</p>
        <div class="message-modal-users">
            
            {foreach $classificationUsers as $classUser}
            <div class="avatar tiny tooltipped" data-position="bottom" data-delay="50" data-tooltip="{if !empty($classUser->name)}{$classUser->name} {$classUser->surname}{/if}">
                <img src="{$classUser->profileImage}" alt="{if !empty($classUser->name)}{$classUser->name} {$classUser->surname}{/if}">
                <input type="hidden" value="{$classUser->id}" name="users[]">
            </div>
            {/foreach}
        </div>
        {/ifset}           
        <div class="form-row">
            {input name}
        </div>
        <div class="form-row">
            {input date}
        </div>
        {*<div class="form-row">
            {input grade}
        </div>*}
    </div>
    <div class="modal-footer clearfix">
        <div class="flex-spacer"></div>
        <div class="form-control">
            <button type="button" class="modal-action modal-close waves-effect waves-light btn-flat">Zrušit</button>
            <button type="submit" class="submit-button waves-effect waves-light btn btn-confirm" disabled>Pokračovat</button>
        </div>
    </div>
    <script>
    $(function() {
        $('select').material_select();
        $("#form-user-classification input[name='name']").on("keyup", function(e) {
           if($(this).val() === "") {
               $("#form-user-classification .submit-button").prop('disabled', true);
           } else {
               $("#form-user-classification .submit-button").prop('disabled', false);
           }
        });
    });
</script>
{/form}